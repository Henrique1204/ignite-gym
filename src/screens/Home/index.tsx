import React from 'react';

import { VStack } from 'native-base';
import HomeHeader from '@components/HomeHeader';

const Home: React.FC = () => {
	return (
		<VStack>
			<HomeHeader />
		</VStack>
	);
};

export default Home;
